# Locked Decisions — Full Detail

The slim, always-on subset lives in the root [`CLAUDE.md`](../CLAUDE.md). This file holds the
complete, detailed decision log grouped by theme. Each entry is a durable constraint — read the
relevant section before touching that subsystem.

## Core Canon (PEARL, UI discipline, vocabulary)
- PEARL should feel ambient, constant, and authoritative — not optional, not a chatbot.
- **PEARL eye never blinks** — eye has look-around and attention moments only.
- Story and learning targets live in mission config, not hardcoded UI logic.
- Avoid too many empty app areas; keep student UI focused.
- Briefing video should be followed by comprehension/activity checks.
- Preserve Week 1 onboarding as the canonical first narrative beat.
- **NEVER touch background images, image sizing, object-fit, or image positioning** without explicit user permission.
- **ALL OfficeView overlays** must use image-space percentages + `imageToViewport()` — never fixed viewport CSS percentages.
- **NEVER change the PEARL sphere colors/style** unless explicitly asked.
- **Vocabulary is TOEIC-first** — `targetWords` in each WeekConfig should prioritize TOEIC-aligned words; world-building/story words are layered in through narrative context. Week 1 is locked as-is (narrative-first exception).
- **pOS digital-first interactions** — task confirmations use `AuthorizationToast` (PEARL eye + progress ring + checkmark), NOT physical rubber stamps. The OS is the authority, not a bureaucrat with a stamp.

## In-World Naming & The Three Verification Siblings
- **"Clarity Check" is the canonical in-world name** for pop-up vocab verifications — reuses the "Clarity" namespace (Clarity Queue, Clarity Tea, Clarity Associates) and mirrors the children's rhyme "Check your words, check your tone."
- **"Compliance Check" is the per-class teacher-scheduled lockout sibling of Clarity Check.** Configured per-class in the Shifts tab (NOT on-demand from ClassMonitor). Teachers pick exact words from a TOEIC-grouped picker; checks fire automatically at chosen placement points (`shift_start | shift_end | after_task`) inside the student's shift cascade. One-shot per `(pair, template)` enforced via DB unique constraint. Different classes can have different templates for the same shift. **Slot rows render INLINE in the Shift Storyboard as cyan dotted-line markers (above the first card, after each task, below the last card)** — `ComplianceCheckMarker` mirrors the `GateMarker` insertion-point pattern. Standalone `ComplianceCheckSlotList` is GONE; do not re-introduce a separate section in `ShiftsTab.tsx`.
- **"Remediation Module" is the behavior-triggered third sibling.** Fires automatically when the rate-trigger state machine in `sessionStore` detects intentional `concernScore` grinding (Stage A warning at +0.4/30s, Stage B modal at +0.7/60s OR second Stage-A within 90s, backstop at score≥3.0). Server-authoritative cooldown `−[0, 0.5, 1.0, 1.5][correctCount]` floored at 0; clawback if grinding resumes within 60s of close (restores cooldown, sets `clawedBack=true`); escalating debounce 90→60→30→0s. Amber accent (vs cyan Clarity/Compliance) so students know the trigger source is behavior, not schedule. PEARL voice stays forced-happy throughout — never punitive. **The "punishment" is more vocab review using words from prior shifts; worst case = student accidentally studied harder.**
- **Concern HUD chip is now clickable** — opens `ConcernTooltip` showing score, threshold band, recent activity from `concernRateBuffer`, threshold-to-next, forced-happy hint. Score drops animate via `useCountDownAnimation` (RAF, ease-out cubic, 1500ms, decrease-only).
- **Compliance Check `after_task` slots are labeled `After {task}` not `Before {task}` (2026-05-08)** — `placement: 'after_task'` with `afterTaskId: X` fires AFTER task X completes (`ShiftQueue.handleComplete` calls `fetchComplianceCheckFor('after_task', completedTaskId)`). Old labels were backwards. Editor modal title + storyboard markers + every consumer of `slotLabel(slot)` must use "After X" — never re-introduce "Before X" for after_task rows.

## Remediation Module Details
- **Remediation study card is lane-aware bilingual (2026-05-08)** — wrong answer in remediation modal renders `RemediationModule.tsx` study card below options: Lane 1 = full bilingual (word + IPA + correct def + Mandarin + example), Lane 2 = same but Mandarin tap-to-reveal, Lane 3 = English-only minimal. Continue button locked 5s with countdown ("Next (5)"). Aligns with Krashen affective filter + Cummins on strategic L1 + Nation/Schmitt on context exposure. Doctrine: forced exposure not punishment, no force-pass loops for ESL.
- **Remediation word source is gated to TOEIC `targetWords` for shifts ≤ current (2026-05-08)** — `pickRemediationWords` in `remediation.ts` intersects candidates with `getComplianceWordsByWeek()` union for weeks 1..weekNumber. Same gate Compliance Check uses; story / world-building / future-shift words are excluded. Bumped `take` from 30 to 100 to leave room for post-filter.

## Backend / Infra (audit batch 2026-05-04 + don't-break rules)
- **Per-class socket scoping (audit batch 2026-05-04)** — Teachers join `class:${classId}` rooms (one per owned class) plus a personal `teacher:${teacherId}` room. The old global `'teacher'` room is GONE; never call `io.to('teacher').emit(...)`. For events tied to a student, look up `classId` via `prisma.classEnrollment.findFirst({ where: { OR: [{ pairId }, { userId }] } })` BEFORE the cascade-delete (or capture from in-scope context), then `io.to('class:' + classId).emit(...)`.
- **JWT_SECRET is mandatory at module load (audit batch 2026-05-04)** — `backend/src/utils/jwt.ts` throws if `process.env.JWT_SECRET` is unset. Fail-fast beats dev-secret in prod. Local dev needs the var set in `backend/.env`.
- **Student login is rate-limited (audit batch 2026-05-04)** — `auth.ts` POST /login uses `express-rate-limit` keyed on IP+designation, 10 attempts per 15 min. Teacher login skipped via `skip` predicate.
- **MissionScore.details merges everywhere (audit batch 2026-05-04)** — `mergeDetails()` helper now duplicated in shifts.ts, submissions.ts, sessions.ts, teacher.ts. ANY new endpoint writing `details: X` MUST read existing details first inside `prisma.$transaction` and merge, or it will clobber writingText/answerLog/pearlFeedback. Flagged for future shared-util refactor.
- **Teacher endpoints check `teacherOwnsPair` (audit batch 2026-05-04)** — single delete, bulk delete, messages (POST /direct, GET /direct/:pairId, POST /:id/thread teacher branch), socket commands (skip/reset/send-to-task) all enforce ownership. Bulk-delete-all-students is now scoped to teacher's classes via `enrollments: { some: { class: { teacherId } } }` — was previously deleting every student in every class.
- **Stale-bundle defense is layered (2026-05-08)** — Layer 1: `frontend/public/serve.json` no-caches `index.html` + `version.json`, long-immutable on hashed `assets/**`. Layer 2: Vite injects `__BUILD_ID__` and writes `dist/version.json`; `useUpdateChecker` polls every 5 min + on tab focus; `UpdateBanner` shows "A new version is available — Reload" on mismatch. Skipped in dev. Backend start script runs `npx prisma migrate deploy && node dist/index.js` so future migrations auto-apply.
- **Teacher live updates are class-room socket events only — never the dead global `'teacher'` room (reconfirmed 2026-05-29)** — all student progress relays through `emitToStudentClass` → `class:${classId}`; the remediation events (`-fired`/`-completed`/`-clawback`) are per-class and ALL THREE are now bound in `useTeacherSocket`. `class:presence` is a class-scoped online-count event. ClassMonitor shows a live "↩ clawed back" badge + a "Concern: X.X" chip (live after a remediation event, else 30s REST via `StudentSummary.concernScore`). The `now`-tick is 5s (live elapsed/flags). ShiftQueue emits its initial task list via `once('connect')` so a cold-open socket doesn't leave the teacher stuck on "Loading task info…". Student cards use a `grid ... items-start` so expanding one card doesn't stretch its row-mates.
- **Swapping a `public/` image needs a filename rename, not in-place replace (2026-05-26)** — `frontend/public/serve.json` caches `/images/**` 1 day on a stable path, so replacing an icon in place leaves clients rendering the old bytes. Rename (e.g. `-v2`) to cache-bust. Applies to every `public/images/*` icon. See [[public-asset-cache-busting]].

## Teacher Dashboard / Scoring Correctness
- **Teacher activity indicator is DB-backed (2026-05-08)** — `Pair.lastSeenAt` and `User.lastSeenAt` columns; `authenticate` middleware bumps the actor's timestamp on every authenticated request, throttled to 60s/actor via in-memory Map (fire-and-forget). `ClassMonitor` derives 4-state ActivityState (`active`/`recent`/`idle`/`offline`) from `online + lastSeenAt + now` and renders matching color + relative-time label. Survives Railway restarts that wipe the in-memory `onlineStudents` Map.
- **Teacher dashboard shows shift progress without click (2026-05-08)** — `GET /api/teacher/students` now parallel-fetches via `buildShiftStatusForPair()` helper (extracted from `/shift-status`) and returns `currentShiftProgress` for every pair. Frontend falls back to `s.currentShiftProgress` when `offlineStatus` has no fresh-fetched entry, so the in-shift line renders on first page load. Click-to-refresh path preserved.
- **Task answer logs surface the actual wrong pick on non-first-try (2026-05-08)** — WordMatch / VocabClearance / ClozeFill all now show `lastWrongPickRef[item]` as `chosen` whenever `wasCorrect` is false (regardless of recovered vs auto-resolved). First-try-correct rows still show the canonical correct text. Removed the unused `autoResolvedRef` distinction.
- **`/api/teacher/students` filters ShiftResults to `completedAt: not null` (2026-05-08)** — Move-to-Shift creates marker `ShiftResult` rows at the target week with `completedAt: null` to record current position; the include in `GET /students` previously had no `where`, so markers inflated `weeksCompleted` by 1 and the ClassMonitor "current shift" highlight off-by-one'd whenever a class had been moved. ANY new query that derives "completed shifts" from `ShiftResult` must filter `completedAt: { not: null }`. Marker rows are NOT completions.
- **`StoryboardStep.taskId` exposes the WeekConfig task `id` (2026-05-08)** — backend storyboard endpoint also returns `task.id` (e.g. `word_match_w3`) alongside `missionType` (e.g. `word_match`). Required for matching Compliance Check `afterTaskId` to storyboard cards on shifts where `task.id ≠ task.type` (W2+).
- **`weeksCompleted` must normalize ShiftResult.weekNumber and Mission.weekId to numeric `weekNumber` (2026-05-19)** — `GET /api/teacher/students` previously used a `Set<string>` mixing `String(sr.weekNumber)` (`"3"`) with `ms.mission.weekId` (`"week-3"`). `ShiftClosing` writes BOTH records on every shift completion, so every real completion contributed two distinct Set entries and `weeksCompleted` was inflated by 1 per real completion, off-by-one'ing the ClassMonitor "Shift N • current" badge. Fix: include `mission.week.weekNumber` and use a `Set<number>`. ANY new endpoint deriving "shifts completed" from BOTH sources MUST use the same pattern — never add `mission.weekId` to a Set that also receives `weekNumber`-derived strings.
- **`MAX_BUILT_WEEK` in `frontend/src/data/narrative-routes.ts` gates which shifts appear in ClassMonitor selectors (2026-05-19)** — bumped 3 → 4 when W4 wiring landed. Bump again when the next week's WeekConfig ships AND backend `ensureQueueMissionsForAllWeeks()` has had a deploy to populate its Mission rows.
- **"On Shift N" emerald pill is always-visible in `ClassMonitor` action row (2026-05-19)** — pre-fix the "Shift N • current" badge only rendered inside the *Move Class to Shift* expanded row; teacher had to open a dropdown just to see where the class was. Pill now sits alongside Pause All / Review Shift / Move Class to Shift. "Review which shift" selector ALSO highlights current with the same emerald+ring pattern that "Move all students to" already had. Source-of-truth unchanged: `currentClassShift = mode(weeksCompleted + 1)` across enrolled students.

## Narrative Structure / Routing / Locations
- **9-shift condensed route is canonical (2026-05-11; applied 2026-05-19)** — `condensedRoute` in `backend/src/data/narrative-routes.ts` AND `frontend/src/data/narrative-routes.ts` is `[1, 2, 3, 4, 5, 6, 11, 14, 18]`. Stale Week 5 bridging entry was deleted in the same commit (it described the pre-redesign Evidence Board W4 and would have misfired on every condensed-route Week 5 entry now that 4 is in-route). Each shift takes ~2 classroom weeks for a Taiwanese high school semester.
- **Briefing video Clip A uses PEARL narration only (2026-05-11)** — no Betty/Ivan/M.K. voiceover in briefing videos. Maximizes contrast for the Unedited hijack (single Party voice → ruptured by a stranger voice hits harder than multiple voices). Other characters speak inside the shift via character message slots + inter-task moments; they do not speak in the briefing.
- **In-world bureaucratic locations, not American-school (2026-05-11)** — citizen activity content uses `Sector N entrance`, `Filing Desk N`, `Common Mess`, `Records Wing`, `Block N Residential`. Avoid: cafeteria, school library, classroom, dormitory. Canon citizens are workers, not students.

## W4 Activity Reconciliation & `[ ].edited` Spy Arc
- **W4 problem RESOLVED via boot-time auto-migration (2026-05-19)** — `backend/src/utils/weekConfigMigrations.ts::ensureQueueMissionsForAllWeeks()` scans every `Week` row on backend boot, looks up its `WeekConfig` by `weekNumber`, and inserts only the missing queue `Mission` rows. Idempotent; mirrors `migrateHarmonyAuthorLabels` pattern. Wired into `src/index.ts` alongside the harmony migration. Any future week with `shiftType: 'queue'` picks up Mission rows automatically on next deploy — no manual seed runs needed against Railway prod. Seed (`backend/prisma/seed.ts:884`) also extended W1-3 → W1-4 + W1-4 unlock for the default class (fresh seeds only).
- **W4 redesigned as Activity Reconciliation (2026-05-11)** — Shift 4 frame swapped from "Evidence Board" to compiling Citizen-4488's Daily Activity Report from 5 surveillance observations. Mid-Doc-Review, Observation E (guest entry logged: Citizen-9020) is reclassified RESTRICTED via a silent visual mutation — NO popup choice (the old `midTaskChoice` field is removed from `week4.ts`). Unedited First Contact is folded INTO Clip A as a hijack at 1:40 (silhouette + modulated voice + `— F` sign-off); the old standalone Clip C + PEARL "TERMINAL ANOMALY" modal are DEPRECATED. The `[ ].edited` hidden app appears on the desktop post-login via a glitch effect (greyed out, "didn't properly download"). The app carries Lexicon (5 Black Words: `truth, mother, freedom, father, name` with all-lanes Mandarin gloss) + Cipher cloze (graded, replaces the old `cloze_fill_w4` content but keeps the task id) + Drop Box (ungraded, post-Shift-Report, NarrativeChoice key `w4_drop_box_first_submission`). End-of-shift recruitment NarrativeChoice modal (`w4_recruitment_vote`: compliant/curious/guarded) gates W5 content depth. Bracket motif `[ ]` everywhere inside the app for redacted content.
- **W4 Black Words are TOEIC B1, not A1 (2026-05-25)** — `witness / relative / individual / independent / private` replace the earlier `truth / mother / freedom / father / name` list. All five name concepts the Party must suppress to function (witnesses contradict revised records, kinship implies bonds outside designation, individuality threatens the collective, independence is the seed of dissent, no private life can exist under total surveillance). Cipher passage in `week4.ts` uses `witness` + `relative` for blanks 3 and 4. Mandarin glosses: 證人 / 親屬 / 個人 / 獨立 / 私人.
- **`[ ].edited` app aesthetic is dead-internet / image-board (2026-05-25)** — plain monospace text on dark slate. NOT collage, NOT hand-scrawled. One font (IBM Mono), two colors (cream-white body + rose-red accents). Mandarin is `> show 中文` tap-to-reveal — every reveal is a small private act. No audio buttons on Lexicon entries. No card chrome, no rounded corners, no gradients. Recruitment modal uses **full-sentence buttons** — the label IS the narrative beat the student signs to (not Yes/Maybe/No).
- **W4 recruitment vote gates W5 (2026-05-25)** — `RecruitmentModal.tsx` writes `w4_recruitment_vote` NarrativeChoice with value `compliant | curious | guarded`. Drop Box writes `w4_drop_box_first_submission` with value `submitted | skipped` + free-text in `context.text`. Both fire between Shift Report and ShiftClosing via the `w4Stage` state machine in `ShiftQueue.tsx` (refresh-safe). W5 must read these to branch Betty/Ivan tones, Citizen-4488 W5 posts, Wellness Division tone, and second `[ ].edited` message depth.
- **Cipher routing: ID-level override in ShiftQueue (2026-05-25)** — `cloze_fill_w4` (by id, not type) routes to `CipherActivity` instead of generic `ClozeFill`. Same scoring + lifecycle, different visual register (dark slate, `[ ]` brackets instead of underscores, Frey signature). Vocab interstitial also bypassed for this one task so Unedited register survives until Ivan's next moment. If a future shift needs a similar single-task chrome override, mirror this pattern (don't extend the TASK_REGISTRY type map for one-off cosmetic changes).
- **`[ ].edited` has NO desktop tile; reached via the FunnelDrawer `[ ]` pill (2026-05-26)** — the redundant large desktop card was removed (it rendered the same `FreyChannel` as the corner pill); `EditedApp`/`FreyChannel`/`FunnelDrawer` are untouched. The W4 archive app's display name is **"Records"** (internal id stays `records-room`). Both are part of the user's insider-spy rebuild — see `Dplan/W4_Edited_App_Spy_Redesign.md`.
- **`[ ].edited` channel is a draggable glitchy contraband WINDOW + the corner `[ ]` launcher is itself draggable (2026-05-27)** — the `FunnelDrawer` panel is no longer a right-anchored side drawer; it's `EditedWindow.tsx`, a floating window dragged by its title bar (Pointer Events → mouse + touch, viewport-clamped, position persisted in a module-level var, **no backdrop** so it floats over the live Party work). The corner `[ ]` pill (`FunnelPill`) is ALSO draggable — drag-to-move; a clean tap (movement < 4px) still opens it; a real drag swallows the trailing click. Deliberately "rough/suspicious" WITHIN the dead-internet doctrine: square corners, dashed hairline border, `⠿` grip, `unsigned build · source unknown`, fake-broken `[–]`/`[▢]` controls (only `[✕]` works), and a bottom **upload bar** that climbs→stalls→retries but never completes. Glitch (`.edited-window`/`.edited-pill` in `index.css`) is paused while dragging and killed by the global `prefers-reduced-motion` rule. Don't re-anchor the drawer to a screen edge, add a tap-to-close backdrop, or remove the pill's drag. Commits `967c03d` + `e536c78` (on master).

## Word Pool (display name; "inscription" internal)
- **Word Pool is a typing RACE, not a quiz (2026-05-25)** — `DrillPromptCard.tsx` rewritten with per-character display (green correct / red wrong / bright block cursor / dim untyped) + hidden `<input>` capturing keystrokes + **auto-advance on exact match**. There is no Submit/Enter button. Overflow chars (past target length) render in red on dim background — explicit `> backspace to correct.` hint appears whenever input is wrong. Eliminates the prior submit-cycle race that left the input frozen after word 1. Don't reintroduce `setFeedback('correct')` patterns in this component.
- **Word Pool standings = horizontal racing track at the TOP (2026-05-25)** — `PoolStandings.tsx` shows each citizen as a row with a fixed 24-cell lane: `▸` completed, `▶` current, `·` remaining. Self bright+glow, leader gets `▲`, others dim. Renders ABOVE the prompt (not below) so competition visibility is constant while typing. Don't simplify back to a single citizen-ID row — that was tried 2026-05-25 morning and lost the competition signal.
- **Word Pool is the display name; "inscription" is the code name (2026-05-25)** — TerminalDesktop tile label and TerminalAppFrame window title say "Word Pool". Backend route remains `/api/inscription/*`, Prisma tables remain `inscription_drills` / etc, component dirs remain `InscriptionPool/`, store remains `inscriptionStore`. Display-only rename keeps the deploy safe. Don't rename internals.
- **Phosphor-green CRT palette for Word Pool (2026-05-25)** — `.crt-phosphor-monitor` (black bg + scanline overlay) + `.phosphor-text*` palette: #33CC66 standard, #66FF99 bright, #1F8540 dim, #0F4F26 faint. Plus `.phosphor-glow` text-shadow. `.pixel-mono` disables anti-aliasing for chunkier monospace. Distinct from `.crt-monitor-screen` (cyan terminal desktop) and the warm Party iOS aesthetic of the shift queue.
- **Hybrid word/sentence drills in Word Pool (2026-05-25)** — `backend/src/utils/inscriptionSentencePool.ts` carries 7 hand-authored in-world sentences per shift (W1–4). `pickInscriptionPrompts()` returns mixed list: ~60% word warm-ups + ~40% sentences for solo/open modes (trial stays words-only). Sentences preferentially use words from the warm-up rounds. New sentences for new shifts go under the new week's key in the same file. A2-B1 syntax, lowercase Party-communiqué register.
- **`useGhostTicker` MUST be throttled, not RAF (2026-05-25)** — original implementation ran a `requestAnimationFrame` loop at 60fps which cascaded re-renders into the parent and choked the input. Now uses `setInterval(100ms)` for ~10fps updates, plenty for the ghost-typing pulse. Don't switch back to RAF without measuring the impact on input responsiveness.
- **Word Pool "Open Pool" is LIVE matchmaking (2026-05-26)** — `backend/src/socket/inscriptionMatchmaking.ts` matches online classmates (same class + lane + week) into a synchronized race via **per-player drills linked by a shared `lobbyId`** (NOT one shared drill row — reuses the dormant `lobbyId` column + `inscription:lobby:*` rooms). In-memory queue forms a pool on cap (`POOL_MAX_PLAYERS=5`) or after `POOL_WAIT_MS=20s`; ghost top-up to `POOL_TARGET_DESKS=4`; a lone player falls back to solo-with-ghosts (still `open`). Live progress broadcasts `inscription:participant-progress` keyed by `pairId` (each client maps it to a local opponent desk). Completion ranks from sibling drills (same `lobbyId`) + snapshots into `InscriptionRecording.pairId` opponent rows (refresh-safe). Open mode is exempt from the 5-min cooldown; Solo keeps cooldown + daily cap. See [[live-open-pool-matchmaking]].
- **Word Pool races show designations (`CA-N`), not random numbers (2026-05-26)** — self + lobby + Roll of Distinction + classmate ghost opponents render the pair's `designation`; Ministry NPCs keep `C-000X`. This REVERSES the old ghost anonymization (deliberate, per user) — don't silently re-anonymize. `InscriptionRecording.citizenNumber` / the API `citizenNumber` field now hold a DISPLAY LABEL (a designation or `C-000X`), NOT a number — don't parse it. See [[inscription-pool-designations]].
- **`TerminalAppFrame` content wrapper MUST stay `flex-1` (2026-05-26)** — the inner wrapper is `relative flex-1 flex flex-col`, giving it a definite height so full-height apps (Word Pool's `crt-phosphor-monitor h-full`) fill the frame instead of letting the cyan `crt-monitor-screen` desktop bg peek through at the bottom. Don't revert it to a bare `relative` (height:auto) wrapper.
- **Word Pool prompt wraps by WORD-unit, never mid-word (2026-05-26)** — `DrillPromptCard` groups the per-character spans into word-units (`inline-flex` nowrap) so the line only breaks between words. Don't flex-wrap the individual char spans (that split words like "sec"/"tor"). Untyped chars use standard `phosphor-text` (not faint) inside a bordered panel for legibility.

## Harmony Content & Mechanics
- **`HarmonyPost.ingestedAt` is the source of truth for NEW-content checks (2026-05-08)** — `createdAt` is intentionally backdated 10 min – 5.5 h by `staggeredCreatedAt()` for narrative texture; never use it to compare against `lastHarmonyVisit`. Use `ingestedAt` (DB-default `now()` at insert) for both `/has-new` count and per-post `isNew` badge.
- **Harmony interactive activities are lane-aware bilingual (2026-05-14)** — Censure Queue, Bulletin Comprehension MCQ, and Archives Vocabulary all follow the [[RemediationModule]] gold-standard pattern: Lane 1 always shows Mandarin, Lane 2 tap-to-reveal, Lane 3 English-only. Backend `DictionaryWord.translationZhTw` already exists; was simply not rendered. The Censure response endpoint returns an optional `studyCard: { word, phonetic, translationZhTw, exampleSentence }` looked up via `lookupStudyWord()` (with `-s/-ed/-ing/-ies` inflection fallback). Bulletins use a read-time enrichment map (`STATIC_TRANSLATION_BY_REF` in `routes/harmony.ts`) so existing DB posts pick up Mandarin without migration — DB value wins on conflict for forward-compat.
- **Censor role now has 5 activity types (2026-05-18)** — `CENSURE_POST_TYPES = ['censure_grammar', 'censure_vocab', 'censure_replace', 'censure_redact', 'censure_triage']`. The two new types add cognitive verbs beyond "spot the error and pick the fix": `censure_redact` is **tap-in-text** (student taps the unapproved word directly inside the post; backend matches `selectedWord` string case-insensitively against `errorWord`); `censure_triage` is **3-bin classification** (Approve / Forward to Wellness / Flag for Reg 14-C). All five render via the same `CensureCard` component with `postType` branching. Static items only for W1-W3 (3 redact + 3 triage per shift, 18 items total); AI generator does not produce these types yet.
- **Censure Triage bins render in fixed order, NOT shuffled (2026-05-18)** — Approve / Forward to Wellness / Flag for Reg 14-C is a taught layout. The Fisher-Yates shuffle in `CensureCard` is skipped when `item.postType === 'censure_triage'`. All other censure types (grammar/vocab/replace) continue to shuffle.
- **Word Redaction is TOEIC-anchored on the approved side only (2026-05-18)** — the *correct* word in every redact pair is a TOEIC target from that shift's `WeekConfig.targetWords` (e.g. `submit`, `inform`, `respond`). The *wrong* word is everyday A2-level English used as an informal foil (`give`, `tell`, `answer`) — these are NOT in `DictionaryWord` and NEVER become teaching targets. StudyCard lookup uses `approvedWord` not `errorWord` for redact items so the Mandarin study card always teaches the TOEIC word.
- **Harmony Feed verdict loop = the Junior Compliance Reviewer (2026-05-29)** — hand-authored "pending" feed posts the student verdicts inline (Approve / Flag). Implemented schema-free: a `feed_review` `postType` (a plain Prisma string — NO migration) whose verdict packet (`correctVerdict` + `violations[]`) rides in the existing `censureData` JSON. The flag modal is 3 steps (cite rule → tap the unapproved word → pick the approved replacement chip). Verdicts persist in `HarmonyCensureResponse` (`@@unique([pairId,postId])` = refresh-safe, no farming); censure-queue stats are unaffected (they filter `CENSURE_POST_TYPES`, which excludes `feed_review`). The answer key (`correctVerdict`/`violations`) is withheld from `/posts` until the post is answered — only `flagOptions` (the MCQ chips) ship. Content in `backend/src/data/harmonyVerdictPosts.ts`. Rules: `reg_14c` (W1+), `conduct_s1` we/us (W2+), `conduct_sentiment` "Conduct Code §5" (W1+).
- **Harmony posts span a Party-stance spectrum (2026-05-29)** — every shift's `feed_review` set runs **praise (Approve) → mild fault → open condemnation (Flag)**. The `conduct_sentiment` rule flags negativity by swapping the negative word for a Party euphemism that **inverts reality on purpose** (`terrible→standard`, `erased→reassigned`) — the satire IS the dystopia and it teaches register vocab. `reg_14c` approved replacements are per-shift TOEIC target words. Condemnation is generic citizen dissent (NOT resistance/Unedited foreshadowing — that's reserved for the W4 spy arc). Tone stays forced-happy; wrong verdicts NEVER feed `concernScore`/Remediation.
- **Harmony is now a goal-driven loop, not a scroll (2026-05-29)** — PEARL goal banner + shift-end **Compliance Report**; **Harmony Credits (HC)** counter (localStorage keyed per pair, awarded for correct verdicts/censure/bulletin + clearing review); **Daily Vocabulary Audit** match-card (`auditPairs` = 3 prior-shift word↔definition pairs from `DictionaryWord`, once/shift); propaganda slogan ticker + Citizen-4488 glitch + PEARL-eye glyph. The feed **drips** (client-side, 15–40s then 30–75s, holds ≤3 newest) with a "↑ N NEW POST" pill. The drip is **index-based** (`displayed = posts.slice(holdCount - revealedCount)`) so filing a verdict NEVER reshuffles the board — don't store post objects in drip state. `/posts` + `/censure-queue` only block on generation for a class's first load; otherwise generate in the background.
- **Citizen-7291 is the proud efficiency-bureaucrat, NOT a distressed complainant (2026-05-29)** — W3 Case 5's complainant was recast to **Citizen-7720** (a non-roster number) because 7291's arc says they don't notice the pattern until W7+. The **Wellness Division** thread set up in W3 (Case 5 reassignment + Ivan's transferred colleague) is paid off in W4 via Ivan's `shift_report` beat tying Citizen-9020's transfer to it — don't drop that callback.

## Design & Aesthetic Guardrails (Design Feedback Log)
- Tone: "forced happy" dystopian, NOT intimidating
- PEARL: kind, welcoming, prominent — user's connection to The Party
- **No "Season" naming** — use in-world bureaucratic terms
- **User HATES monochrome green CRT everywhere** — green CRT only inside terminal view
- **User HATES dark CRT terminal for shift queue** — shift queue is a "forced happy" government iOS app, NOT a hacker terminal
- **Office view = warm retrofuturist** — pastels, neon accents, chrome
- **TerminalAppFrame stays dark** (device chrome), content area uses `crt-monitor-screen` cyan CRT background
- **Terminal desktop & app frame** both use `crt-monitor-screen` class for cyan CRT background with scan line effect
- **pOS digital-first, not physical stamps** — task confirmations use `AuthorizationToast` (PEARL eye + progress ring + checkmark), not BureauStamp. "The OS is the authority, not a bureaucrat with a stamp." StampChoice (CHANGED/REMOVED classification) still uses old stamp pattern.
- **AuthorizationToast image**: `pearl-eye-glow.png` (128x128, 22KB) — radial-gradient mask for feathered edges into transparency
