# Daily Log — 2026-06-03

Two threads this session: a **CLAUDE.md slim-down** (split the bloated root instruction file into themed `docs/` files, verified lossless by an audit workflow), and the headline build — **Shift 4's Cipher Decryption rebuilt as a 3-document redacted-reveal** that uploads each restored record to the `[ ].edited` app. Plus a diagnosis of the local-dev login failure (missing `JWT_SECRET`). Two commits fast-forwarded to `master`.

**Commits (fast-forwarded to `master`):**
- `94067a8` — docs: slim CLAUDE.md into themed `docs/` files
- `57465e9` — feat(w4): multi-document Cipher Decryption + upload to `[ ].edited`

Builds (`frontend tsc -b` + Vite, `backend tsc` + 55 vitest) verified green before push.

---

## 1. CLAUDE.md slim-down (`94067a8`)

Root `CLAUDE.md` had grown to **166 lines / 42 KB**, dominated by a ~64-entry Locked Decisions block plus status/backlog sections that read more like changelog logs than always-on instructions. Split it:
- **`docs/locked-decisions.md`** — the full themed decision log (every bullet preserved verbatim).
- **`docs/code-map.md`** — Harmony data files + task-result/scoring utilities.
- **`docs/next-work.md`** — Harmony expansion phases + the Next Work backlog.
- Root kept Vision, Quick Reference, a compressed **Locked Decisions — Core** (always-on canon), and an expanded Detail Files index. **49 lines now (−87%).**

A 7-agent audit workflow diffed `git HEAD:CLAUDE.md` against the new doc set token-by-token: **zero drops, zero meaning-alterations** across all 64 decisions + every section. The root-quality critic flagged one always-on rule worth promoting back — **"never score grammar on open writing"** — added as a root one-liner.

## 2. Shift 4 — multi-document Cipher Decryption (`57465e9`)

The headline. Four design picks from the user up front: **new separate content** (the optional Records Room snoop layer untouched) · arc **9020 → cover-up → 4488** · **redacted-reveal** mechanic · **per-doc upload button + transfer**.

`cloze_fill_w4` ("Cipher Decryption"), previously a single 5-blank cloze, is now **three personal "records" documents** (`config.documents[]`) the student restores and uploads to `[ ].edited`:
- **DOC 1 — PERSONNEL FILE / CITIZEN-9020:** restores who he was — a `witness`, a `relative` (a daughter), a `private` life. *"They kept the badge scan. It deleted the man."*
- **DOC 2 — RECORD ADJUSTMENT LOG / CITIZEN-9020:** the record before the rewrite — `independent`, an `individual`, a `witness`. The Party falsifies, not just deletes.
- **DOC 3 — CASE FLAG (sealed) / CITIZEN-4488:** the citizen reconciled all day is flagged for `reassignment`; nothing they keep is `private`. The watcher becomes the watched.

All 5 Black Words covered (witness×2, private×2, individual×2, relative×1, independent×1) + the `reassignment` euphemism. Each blank's distractors include a cold Party word to teach by contrast.

**Mechanic (`CipherActivity.tsx`, full rewrite):** each blank is a redacted `████` block; tap it → pick the restored word from that blank's own `options`. Lane-aware attempts (3/2/1) + forced-exposure auto-reveal (no force-fail loop). Multi-doc state machine (`solving → restored → uploading → sent` per doc); scores once at the end (`firstTryCorrect / totalBlanks`, vocab category).

**Upload:** restore a doc → `▸ upload to [ ].edited` → transfer animation → writes **NarrativeChoice `w4_cipher_<id>` = 'restored'** (`context.intel`) via new `spyStore.uploadCipherDoc` (idempotent — skips the POST if already restored; the route inserts, not upserts). Surfaces in **Frey's channel** under a new **"the files you restored"** section. `EditedWindow` upload-bar count now includes cipher uploads. The `document_review` bridge foreshadows "three files."

**Review:** a 4-agent adversarial workflow (state-machine / integration / narrative-pedagogy / answer-key). The narrative pass shipped with strong praise. Fixed 2 real **answer-key ambiguities** (`complaint` and `informer` were defensibly-correct distractors → reworded + rebalanced Black Words). Added timer-cleanup (unmount/skip safety), an idempotent-upload guard, and a dev config guard. Traced and **skipped 3 non-reachable "bugs"** (cross-doc timeout bleed, double-interval upload, double-`onComplete` on refresh).

**Doctrine captured:** redacted-reveal answer-keys must anchor the correct word with apposition/definition (*"never a witness, never a person who saw"*) — open frames (*"never a ___ at all"*) accept many nouns and produce defensible wrong-answer disputes.

## 3. Local-dev login diagnosis

The reported "can't log in as teacher / CA-1" is **not a credentials problem** — the local backend crash-loops at boot because `backend/.env` is **missing `JWT_SECRET`** (the mandatory-at-module-load guard in `jwt.ts`). No backend → no API → every login fails. The fix is one line in the local `.env` (the safety layer blocks me from writing it, so it's left for the user). The deployed site has its own Railway `JWT_SECRET`, so prod login is unaffected. Frontend dev server came up fine on `http://localhost:3000`.

---

## Open / next
- **Local `backend/.env` needs `JWT_SECRET`** for local dev to run (deployed site unaffected).
- **Clip A + Clip B Canva videos** remain the only non-code W4 work.
- Stray untracked `frontend/src/components/terminal/apps/EditedApp/EditedWindow 2.tsx` (Finder/sync cruft) — safe to delete.
- `Dplan/Project_Update_Log.md` appears deprecated (last updated 2026-04-30; superseded by the `Daily_*.md` + `changelog.md` convention) — confirm whether it should still be maintained.
- Reminder: **pushed ≠ deployed** — confirm both Railway services rebuild + the frontend bundle hash changes; reach the cipher via teacher → Move Class to Shift 4.
